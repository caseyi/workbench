- [ ] **Re-run OGC bug revalidation on the new indonesiademo build** - deferred until the new demo-silnas version deploys (Casey will kick it off). This session (indonesiademo, demo-silnas v3.2.1.10) closed OGC-508/473/452/525 and left 3 bugs to recheck on the new build: OGC-515 (rejection->NCE / Rejection Report) — the 503 is already gone and it's root-caused as two by-design rejection flows, so do the live round-trip via the REAL UI (Claude-in-Chrome): use /SampleRejection to create an NcEvent and confirm it shows in Non-Conforming Events (the legacy JSP form 500s via fetch injection — needs the UI + a configured project), plus a result-level test rejection to confirm it lands in the Rejection Report, then close or re-scope as enhancement; OGC-819 (SampleTypeRequestDAO Integer-vs-String PK) and OGC-509 (global-search has no zero-results empty state) both still present in develop, recheck. Also re-verify the 5 open epic OGC-527 children (OGC-1048/1050/1051/1059/1060) against the new build. TestModify live-save POST technique + non-atomic/slow-write gotchas saved in memory reference_testmodify_live_save.md. NEXT: when the build is up, log into indonesiademo and run the OGC-515 UI rejection round-trip first #qa @vector @jira-admin (ref: OpenELIS QA Jira triage - Indonesia/Env-Vector, epic OGC-527) (from: OpenELIS QA bug triage) (touched: 2026-06-22)
